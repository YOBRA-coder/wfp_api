# ── Server ──────────────────────────────────────────────────────────────────
FRONTEND_URL=http://localhost:5173

# ── Stripe (card payments — international users) ────────────────────────────
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional: create these as real Stripe Price IDs (Dashboard > Product catalog)
PRICE_TRADER_PRO=price_...
PRICE_TRADER_ELITE=price_...
PRICE_PROVIDER_BASIC=price_...
PRICE_PROVIDER_PRO=price_...

# ── M-Pesa Daraja (STK Push — Kenyan users) ──────────────────────────────────
MPESA_ENV=sandbox                # sandbox | production
MPESA_CONSUMER_KEY=...
MPESA_CONSUMER_SECRET=...
MPESA_SHORTCODE=174379           # your Paybill/Till number in production
MPESA_PASSKEY=...
MPESA_CALLBACK_URL=https://your-deployed-backend/payments/mpesa/callback

# ── Pricing (override defaults if you want) ──────────────────────────────────
REGISTRATION_FEE_USD=2.00
REGISTRATION_FEE_KES=250

# ── Telegram Bot ──────────────────────────────────────────────────────────────
# 1. Message @BotFather on Telegram, /newbot, follow the prompts
# 2. Paste the token it gives you below, and its @username (no @)
# 3. Run the bot as its own process: python telegram_bot.py
TELEGRAM_BOT_TOKEN=8719051961:AAGm9q124QT0nLM7MVEo8Koozvu6zBpQnPo
TELEGRAM_BOT_USERNAME=nexusfxsignals

# ── Web Push (PWA notifications) ─────────────────────────────────────────────
# Generate your own pair once — NEVER reuse any example keys in production:
#   python3 -c "
#   from py_vapid import Vapid02
#   import base64
#   v = Vapid02(); v.generate_keys()
#   pn = v.public_key.public_numbers()
#   raw = b'\x04' + pn.x.to_bytes(32,'big') + pn.y.to_bytes(32,'big')
#   print('VAPID_PUBLIC_KEY=' + base64.urlsafe_b64encode(raw).decode().rstrip('='))
#   priv = v.private_key.private_numbers().private_value.to_bytes(32,'big')
#   print('VAPID_PRIVATE_KEY=' + base64.urlsafe_b64encode(priv).decode().rstrip('='))
#   "
VAPID_PUBLIC_KEY=...
VAPID_PRIVATE_KEY=...
VAPID_CLAIMS_EMAIL=mailto:support@yobbytech.com
